define 'view', [], ->
    class View
        constructor: (urlRegex = null) ->
            @urlRegex = urlRegex

        present: ->

        dismiss: ->

        getTitle: ->
            ''

    View
